import React from 'react';
import styled from 'styled-components';

const StyledNav = styled.nav`
  height: 64px;
  width: 100vw;
  background-color: #ed2a7e;
`;

const Nav = () => {
  return <StyledNav>RaspPiWeather</StyledNav>;
};

export default Nav;
